model:
  name: nemotron-speech-streaming-en-0.6b
  pretrained: null

data:
  manifest: data/subset/manifest.csv
  sample_rate: 16000

tokenizer:
  path: tokenizers/
  vocab_size: 8000

training:
  epochs: 5
  batch_size: 8
  lr: 1e-4
